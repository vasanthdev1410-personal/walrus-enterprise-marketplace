name: Deploy Staging Foundation
on: { workflow_dispatch: {} }
permissions: { contents: read, id-token: write }
jobs:
  approval-required:
    environment: staging
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment foundation only. Product Owner approval and approved AWS inputs are required before enabling deployment."
