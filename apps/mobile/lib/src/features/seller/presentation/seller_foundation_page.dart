import 'package:flutter/material.dart';

class SellerFoundationPage extends StatelessWidget {
  const SellerFoundationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Seller boundary')),
      body: const Center(
        child: Text('Seller functionality requires a future approved module.'),
      ),
    );
  }
}
