# WALRUS Mobile Foundation

One Flutter application supports isolated customer and seller feature boundaries. Authentication,
permissions, role selection, role-aware navigation, and all business features remain future work.
Admin and super-admin functionality are prohibited in this application.

Run `flutter pub get`, `flutter analyze`, and `flutter test`. Android and iOS platform runners must be
generated and validated using the approved Flutter stable SDK before device builds are accepted.
